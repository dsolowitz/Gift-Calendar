create table EVENT(
                      TITLE VARCHAR(255),
                      START DATETIME,
                      END DATETIME,
                      DESCRIPTION VARCHAR(255)

);

create table QUESTIONNAIRE(
	ID INT,
	NAME VARCHAR(50),
	GENDER VARCHAR(10),
	AGE VARCHAR(10),
	HOBBIES VARCHAR(255),
	MOTIVATION VARCHAR(255),
	PRICING VARCHAR(20),
	CATEGORY VARCHAR(255),
	HOLIDAY VARCHAR(40),
	WEB_PREFERENCE VARCHAR(40),
	CUSTOMIZED VARCHAR(20),
	DAYS_IN_ADVANCE INT
);